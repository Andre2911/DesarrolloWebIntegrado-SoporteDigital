import { AsignacionesRouter } from './asignaciones.router';

describe('AsignacionesRouter', () => {
  it('should create an instance', () => {
    expect(new AsignacionesRouter()).toBeTruthy();
  });
});
